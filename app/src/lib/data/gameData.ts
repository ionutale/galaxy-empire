/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable no-unused-vars */
export const BUILDING_DATA = {
  controlCenter: { name: 'Control Center', description: 'The heart of your colony. Upgrading unlocks new buildings and expands build slots.', cost: (level: number) => ({ metal: Math.round(100 * Math.pow(1.8, level)), crystal: Math.round(50 * Math.pow(1.8, level)) }), time: (level: number) => Math.round(60 * Math.pow(1.5, level)), benefit: (level: number) => `Unlocks new building options.` },
  metalMine: { name: 'Metal Mine', description: 'Extracts and refines metal ore from the planet\'s crust.', cost: (level: number) => ({ metal: Math.round(60 * Math.pow(1.5, level)), crystal: Math.round(15 * Math.pow(1.5, level)) }), time: (level: number) => Math.round(45 * Math.pow(1.6, level)), production: (level: number) => Math.round(30 * level * Math.pow(1.1, level)), benefit: (level: number) => `Production/hr: ${Math.round(30 * level * Math.pow(1.1, level))}` },
  crystalSynthesizer: { name: 'Crystal Synthesizer', description: 'Grows valuable crystals used in advanced electronics and research.', cost: (level: number) => ({ metal: Math.round(48 * Math.pow(1.6, level)), crystal: Math.round(24 * Math.pow(1.6, level)) }), time: (level: number) => Math.round(50 * Math.pow(1.6, level)), production: (level: number) => Math.round(20 * level * Math.pow(1.1, level)), benefit: (level: number) => `Production/hr: ${Math.round(20 * level * Math.pow(1.1, level))}` },
  deuteriumRefinery: { name: 'Deuterium Refinery', description: 'Synthesizes Deuterium, the fuel for your fleets.', cost: (level: number) => ({ metal: Math.round(225 * Math.pow(1.5, level)), crystal: Math.round(75 * Math.pow(1.5, level)) }), time: (level: number) => Math.round(120 * Math.pow(1.7, level)), production: (level: number) => Math.round(10 * level * Math.pow(1.1, level)), benefit: (level: number) => `Production/hr: ${Math.round(10 * level * Math.pow(1.1, level))}` },
  shipyard: { name: 'Shipyard', description: 'Constructs your fleets and defensive structures.', cost: (level: number) => ({ metal: Math.round(400 * Math.pow(2, level)), crystal: Math.round(200 * Math.pow(2, level)) }), time: (level: number) => Math.round(300 * Math.pow(1.8, level)), requires: { building: 'controlCenter', level: 2 }, benefit: (level: number) => `Reduces ship construction time.` },
  researchLab: { name: 'Research Lab', description: 'Unlocks powerful new technologies for your empire.', cost: (level: number) => ({ metal: Math.round(200 * Math.pow(2, level)), crystal: Math.round(400 * Math.pow(2, level)) }), time: (level: number) => Math.round(300 * Math.pow(1.8, level)), requires: { building: 'controlCenter', level: 1 }, benefit: (level: number) => `Reduces research time.` },
  terraformer: { name: 'Terraformer', description: 'Improves planet habitability and increases maximum population/production.', cost: (level: number) => ({ metal: Math.round(1000 * Math.pow(1.9, level)), crystal: Math.round(800 * Math.pow(1.9, level)) }), time: (level: number) => Math.round(600 * Math.pow(1.7, level)), requires: { building: 'controlCenter', level: 3 }, benefit: (level: number) => `Increases base production by ${5 * level}%` },
  naniteFactory: { name: 'Nanite Factory', description: 'Speeds up construction and repairs using nanotechnology.', cost: (level: number) => ({ metal: Math.round(1500 * Math.pow(2, level)), crystal: Math.round(1200 * Math.pow(2, level)) }), time: (level: number) => Math.round(900 * Math.pow(1.8, level)), requires: { building: 'shipyard', level: 2 }, benefit: (level: number) => `Reduces build times by ${Math.min(50, 5 * level)}%` },
  fusionReactor: { name: 'Fusion Reactor', description: 'Provides large amounts of energy to power advanced systems.', cost: (level: number) => ({ metal: Math.round(800 * Math.pow(1.8, level)), crystal: Math.round(600 * Math.pow(1.8, level)), deuterium: Math.round(200 * Math.pow(1.6, level)) }), time: (level: number) => Math.round(700 * Math.pow(1.7, level)), benefit: (level: number) => `Increases energy cap and improves efficiency.` },
  missileSilo: { name: 'Missile Silo', description: 'Houses defensive missiles to protect your colonies.', cost: (level: number) => ({ metal: Math.round(500 * Math.pow(1.6, level)), crystal: Math.round(300 * Math.pow(1.6, level)) }), time: (level: number) => Math.round(400 * Math.pow(1.6, level)), requires: { building: 'controlCenter', level: 2 }, benefit: (level: number) => `Adds anti-ship defensive capability.` },
  sensorPhalanx: { name: 'Sensor Phalanx', description: 'Long-range sensors that reveal fleet movements and cloaked targets.', cost: (level: number) => ({ metal: Math.round(600 * Math.pow(1.7, level)), crystal: Math.round(600 * Math.pow(1.7, level)) }), time: (level: number) => Math.round(450 * Math.pow(1.6, level)), requires: { research: 'laserTechnology', level: 1 }, benefit: (level: number) => `Increases detection range and intel accuracy.` }
};

export const CHIP_DATA = {
  attackChip: { name: 'Attack Chip', description: 'Increases ship attack by 10% per level.', cost: (level: number) => ({ crystal: Math.round(500 * Math.pow(1.7, level)), deuterium: Math.round(200 * Math.pow(1.6, level)) }), time: (level: number) => Math.round(300 * Math.pow(1.6, level)), effect: (level: number) => `+${10 * level}% attack` },
  defenseChip: { name: 'Defense Chip', description: 'Increases ship defense by 10% per level.', cost: (level: number) => ({ metal: Math.round(400 * Math.pow(1.7, level)), crystal: Math.round(400 * Math.pow(1.7, level)) }), time: (level: number) => Math.round(300 * Math.pow(1.6, level)), effect: (level: number) => `+${10 * level}% defense` },
  speedChip: { name: 'Speed Chip', description: 'Improves ship travel speed (reduces travel time).', cost: (level: number) => ({ crystal: Math.round(600 * Math.pow(1.6, level)), deuterium: Math.round(300 * Math.pow(1.5, level)) }), time: (level: number) => Math.round(400 * Math.pow(1.5, level)), effect: (level: number) => `-${5 * level}% travel time` },
  cargoChip: { name: 'Cargo Chip', description: 'Increases cargo hold efficiency for fleets.', cost: (level: number) => ({ metal: Math.round(300 * Math.pow(1.5, level)), crystal: Math.round(500 * Math.pow(1.5, level)) }), time: (level: number) => Math.round(350 * Math.pow(1.5, level)), effect: (level: number) => `+${10 * level}% cargo` }
};

export const SYSTEMS_DATA = {
  trade: { name: 'Trade System', description: 'Enables resource trading, market orders, and price discovery.' },
  espionage: { name: 'Espionage', description: 'Spy on other players, steal limited intel and sabotage operations.' },
  diplomacy: { name: 'Diplomacy', description: 'Formalize alliances, treaties and war declarations.' },
  automation: { name: 'Automation', description: 'Enable automation scripts: auto-build, auto-research and simple macros.' }
};
